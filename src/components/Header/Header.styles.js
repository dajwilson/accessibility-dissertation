import { styled } from "styled-components";

const HeaderContainer = styled.div`
  position: sticky;
  top: 0;
  width: 100%;
  display: flex;
  flex-direction: row;
  justify-content: space-around;
  background-color: pink;
`;

const s = { HeaderContainer };

export default s;
